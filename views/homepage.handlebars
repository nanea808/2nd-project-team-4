<div class="container text-center">
    <div class="row align-items-center">
        {{#each groups as |group|}}
            <div class="col">
                <p>{{group.title}}</p>
            </div>
        {{/each}}
    </div>
</div>
